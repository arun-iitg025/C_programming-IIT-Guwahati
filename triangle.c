#include<stdio.h>
int main(){
         float l, b, area;
          printf("enter two numbers\n");
          scanf("%f%f", &l,&b);
          area=0.5*l*b;
          printf("area=%f", area);
             return 0;
}